// cargo build builds
// cargo run builds and then executes
// cargo check checks for compile errors
// cargo build --release to compile with optimization

fn main() {
    println!("Hello, tob 2!");
}
